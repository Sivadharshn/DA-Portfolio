CREATE TABLE listings (
    id text,

    listing_url TEXT,
    name TEXT,
    description TEXT,
    host_id BIGINT,
    host_name TEXT,
    host_since text,
    host_location TEXT,
    host_response_time TEXT,
    host_response_rate TEXT,
    host_acceptance_rate TEXT,
    host_is_superhost BOOLEAN,
    host_listings_count INTEGER,
    host_total_listings_count INTEGER,
    host_verifications TEXT,
    property_type TEXT,
    room_type TEXT,
    accommodates INTEGER,
    bathrooms NUMERIC(3,1),
    bathrooms_text TEXT,
    bedrooms NUMERIC(3,1),
    beds NUMERIC(3,1),
    number_of_reviews INTEGER,
    review_scores_rating NUMERIC(3,2))

ALTER TABLE airbnbcase1.listings
ALTER COLUMN host_since
TYPE DATE
USING host_since::DATE;

ALTER TABLE airbnbcase1.reviews
ALTER COLUMN listing_id TYPE BIGINT
USING listing_id::NUMERIC::BIGINT;